import { GlobalExceptionFilterFilter } from './global-exception.filter';

describe('GlobalExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new GlobalExceptionFilterFilter()).toBeDefined();
  });
});
